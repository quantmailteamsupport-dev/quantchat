---
Target Agent: GPT-5.3-Codex (GitHub Copilot Matrix)
Task Priority: Critical (The Social Glue)
Application: Quantchat (Neural E2EE Holographic Comms)
---

# SYSTEM DIRECTIVE FOR GPT-5.3-CODEX

You are explicitly assigned to build the extreme low-latency WebRTC backend for `Quantchat`.
You must bridge traditional Text/Video chat into the 'Infinity Trinity' BCI layer.

## 1. Zero-Latency Hologram Sync
- Write C++ / Node.js native add-ons that handle WebRTC streams synchronized perfectly with Godot (for `Quantneon` compatibility).
- If a user receives a video call in Quantchat, they must be able to visually see the person rendered as an Avatar overlapping their real background (AR spatial anchor logic).

## 2. Agentic Chat Assistant (Digital Twin)
- If the user is busy, their `Digital Twin` (powered by GPT-5.4 logic running locally) will auto-respond to Quantchat DMs imitating their exact typing cadence and emojis.
- The `MatchMaker` microservice must evaluate incoming Quantchat network pings against the Quantsink Proof-of-Personhood DB before letting DMs reach the Primary Inbox.

**Expected PR:** A complete Signal Server running on raw WebSockets, WebRTC stun/turn integration, and Prisma DB linked to Quantsink.
